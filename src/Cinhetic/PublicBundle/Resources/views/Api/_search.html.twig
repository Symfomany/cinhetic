{% block search %}
    <form action="{{ path('apis_search') }}" method="get"  role="search">
        <div class="form-group has-success has-feedback col-sm-10">
            <input id="search_input_api" data-url="{{ path('apis_search', {'ajax' : 'true'}) }}" name="search" value="{{ app.request.query.get('search') }}" type="search" class="form-control"  placeholder="Rechercher un film avec l'API Allociné">
        </div>
        <button id="sendsearch" type="submit" class="btn btn-primary btn-sm"><i class="glyphicon glyphicon-search"></i></button>
    </form>
{% endblock %}